import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'ecart';
}
//Use the Mock Json server for backend npm install json server and I have a database in name of db.json here after 
//install json server into the server folder I made the db.json with mock data after that we change package.json inside the server folder the test to start: add your db.json name it is your db server 
//for start the server go server folder and npm start
//Modals are small pop-up windows which are really handy when you want to display to the user extra content, configuration or consent request.-->
 //Interface is a specification that identifies a related set of properties and methods to be implemented by a class.
